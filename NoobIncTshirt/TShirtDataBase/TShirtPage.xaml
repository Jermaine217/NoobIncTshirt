<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="NoobIncTshirt.Page1">
    <ScrollView>
        <StackLayout Margin="20" VerticalOptions="StartAndExpand">
            <Label Text="Name" TextColor="Red" />
            <Entry Text="{Binding Name}" />
            <Label Text="Size" TextColor="Blue" />
            <Entry Text="{Binding Size}" />
            <Label Text="ShippingAddress" TextColor="Red" />
            <Entry Text="{Binding ShippingAddress}" />
            <Label Text="Done" TextColor="Blue" /> 
            <Switch IsToggled="{Binding Done}" />
            <Button Text="Save" Clicked="OnSaveClicked" TextColor="Blue"/>
            <Button Text="Delete" Clicked="OnDeleteClicked" TextColor="Red" />
            <Button Text="Cancel" Clicked="OnCancelClicked" TextColor="Blue" />
            <Button Text="View Orders" Clicked="OrdersCliked" TextColor="Red"/>
              
         


        </StackLayout>
    </ScrollView>
</ContentPage>
